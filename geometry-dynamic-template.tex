\geometry{
    margin=${margin}cm,
    top=${top}cm,
    bottom=${bottom}cm,
    % showframe,
}

\setlength\tabcolsep{${tabcolsep}pt}
\setlength\parskip{${parskip}pt}
\renewcommand\arraystretch{${arraystretch}}

\newcommand\leftcol{${leftcol}}
\newcommand\rightcol{${rightcol}}